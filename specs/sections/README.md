# Шина данных — техническое задание (разделено по разделам)

Каждый раздел `nexus_spec.md` вынесен в отдельный файл. Исходный сводный файл сохранён в [../nexus_spec.md](../nexus_spec.md).

## Оглавление

| № | Файл | О чём |
|---|---|---|
| 1 | [01-purpose.md](01-purpose.md) | Назначение |
| 2 | [02-architecture.md](02-architecture.md) | Архитектура верхнего уровня |
| 3 | [03-receiver.md](03-receiver.md) | Receiver Service — эндпоинты, семантика, конфиг узла, URL-режимы, авторизация, состояния |
| 4 | [04-sender.md](04-sender.md) | Sender Service — gRPC API, устройство, логирование в ClickHouse |
| 5 | [05-storage.md](05-storage.md) | Хранилища: PostgreSQL, ClickHouse, Kafka, Redis, шифрование |
| 6 | [06-metrics.md](06-metrics.md) | Метрики и наблюдаемость |
| 7 | [07-web-ui.md](07-web-ui.md) | Веб-интерфейс — экраны, аутентификация, audit log, API-токены |
| 8 | [08-config.md](08-config.md) | Конфигурация приложения — файлы, env, `config.yml` |
| 9 | [09-resilience.md](09-resilience.md) | Высоконагруженность и отказоустойчивость |
| 10 | [10-testing.md](10-testing.md) | Тестирование — уровни, сценарный тест |
| 11 | [11-swagger.md](11-swagger.md) | Swagger / OpenAPI |
| 12 | [12-repo-structure.md](12-repo-structure.md) | Структура репозитория |
| 13 | [13-build-run.md](13-build-run.md) | Сборка и запуск — Makefile, Docker Compose |
| 14 | [14-logging-sentry.md](14-logging-sentry.md) | Логирование и Sentry |
| 15 | [15-acceptance.md](15-acceptance.md) | Критерии приёмки |
| 16 | [16-out-of-scope.md](16-out-of-scope.md) | Out of scope в v1 / планы на v2 |
| 17 | [17-patterns.md](17-patterns.md) | Паттерны разработки (Clean Architecture, фронтенд) |
| 18 | [18-multi-tenancy.md](18-multi-tenancy.md) | Multi-tenancy v2 — команды, изоляция, CH-БД per team, перенос узлов |
| 19 | [19-ch-templates.md](19-ch-templates.md) | Шаблоны запросов ClickHouse — каталог DDL, CODEC/индексы/TTL, авто-создание таблицы узла |
| 20 | [20-notifications.md](20-notifications.md) | Уведомления операторам в Telegram — cron-расписание, ошибки узлов, тестовая отправка |
| 21 | [21-ui-redesign.md](21-ui-redesign.md) | Редизайн UI под эталон (дизайн-токены, UI-kit, app-shell) + HTTP-API метрик панели (Prometheus + ClickHouse) |
| 22 | [22-logging-controls-cards.md](22-logging-controls-cards.md) | Контроль логирования узла (тумблер, обрезка тел), раскладка карточками Overview, перевод Telegram-алертов на Prometheus |
| 23 | [23-allowed-hosts-catalog.md](23-allowed-hosts-catalog.md) | Каталог разрешённых хостов (SSRF) — общий справочник exact/wildcard/regex, привязка к узлам, preview, denорм-снимок |
| 24 | [24-headers-catalog.md](24-headers-catalog.md) | Справочник HTTP-заголовков — combobox с автодополнением и автосозданием, usage_count on-read |
| 25 | [25-topbar-swagger.md](25-topbar-swagger.md) | Swagger в шапке — два дока (Receiver + Web), popover, раздача обоих Web-бинарём |
| 26 | [26-roles-access-control.md](26-roles-access-control.md) | RBAC — три роли (Admin/Manager/Viewer), иерархия рангов, матрица доступа, self-service смена своего пароля |
| 27 | [27-rabbitmq-async.md](27-rabbitmq-async.md) | Тип узла RabbitMQAsync — Puller-воркер RabbitMQ→Kafka, поля `rmq_*`/`pull_*`, runtime-`degraded`, `POST /api/nodes/test-rmq`, метрики, UI, сценарные тесты |
| 28 | [28-online-metrics.md](28-online-metrics.md) | Онлайн-метрики и UX — публичный адрес приложения, онлайн-обновление метрик, период просмотра (1h..30d+календарь), маскирование данных авторизации, понятные ошибки валидации, фильтр RabbitMQAsync, багфиксы стенда (CH-таблица, резолв async) |
| 29 | [29-node-comment.md](29-node-comment.md) | Комментарий узла — текстовое описание для команды (поле `comment`, форма/обзор узла), UI-метаданные вне маршрутизации |
| 30 | [30-logging-panic-recovery.md](30-logging-panic-recovery.md) | Логирование, обработка паник и идентификация запросов — `safego.Recover` во всех горутинах, кастомный gin-recovery (500 + лог + Sentry), сквозной `request_id` (UUID v4, `X-Request-Id`) в логах/Sentry, версия приложения в футере SPA (`GET /api/version`), bump логгера до v1.7.9 (`WithContext`) |
| 31 | [31-kafka-monitoring.md](31-kafka-monitoring.md) | Мониторинг Kafka — admin-only alarm-dashboard `/kafka` (в блоке Аудита, не в Настройках): health-banner, KPI, throughput/lag графики (recharts), таблица топиков, top-узлы, брокеры. API `/api/kafka/{overview,timeseries,topics,by-node,test}` из Prometheus (`method="requestAsync"`) + Kafka Admin (`segmentio/kafka-go`, кеш Redis 30с), мягкая деградация, rate-limit 60/мин |
| 32 | [32-loop-protection.md](32-loop-protection.md) | Защита от зацикливания — служебный hop-счётчик `X-Nexus-Hops` (инкремент на каждом проходе через шину, обрыв при `receiver.max_hops`, дефолт 5 → **508 Loop Detected**, sync+async), self-reference валидация `target_url` при сохранении узла (`web.self_ingress_hosts`), метрика `nexus_loop_detected_total` |
| 33 | [33-chart-tooltips.md](33-chart-tooltips.md) | Доработка тултипов графиков — единый презентационный `<ChartTooltip>` (период → главное значение → серии с маркерами → подвал/дельта → действие) на recharts + Radix **без новых зависимостей**, интеграция во все графики (TrafficChart, Throughput/Lag Kafka, MiniSpark, Sparkline Overview), action «открыть логи за момент», i18n; partition-lag и «неделю назад» — out-of-scope (нет данных) |
| 34 | [34-ops-session-version-async-queue.md](34-ops-session-version-async-queue.md) | Операбельность — «Настройки» вниз сайдбара; настраиваемая длительность сессии (app_settings + динамический провайдер TTL + hot-reload, валидация 5мин..30сут); обогащённый `GET /api/version` (commit/build_date) + dev-only override версии (гейт `web.allow_version_override`, прод запрещён); управление async-очередью Kafka на узле requestAsync (глубина/список 50+тело peek'ом, удаление одного/период/всё через Redis-tombstones `qcancel:<id>`, проверка в Sender, `/api/nodes/:id/async-queue/*`); фикс replay 405 (слать `IncomingMethod`, не залогированный `OutgoingMethod`) |
| 35 | [35-queue-tab-rework.md](35-queue-tab-rework.md) | Переработка вкладки «Очередь» (перерабатывает §34.4/§34.6) — реальная модель потока (enabled+мёртвый адрес → DLQ, не живая очередь); «неудачные доставки» из ClickHouse-логов (`done=0`, `CountFailed` + `/logs/failed-count`, переиспользование списка/тела/replay) вместо дорогого Kafka-DLQ-peek (удалён); честная семантика (живая очередь — tombstone-purge admin; неудачи — фильтр+пауза+replay, без фейк-очистки); `PATCH /nodes/:id/status` (пауза/отключение, manager+); RBAC-фикс (failed-view viewer+, управление очередью admin); KPI-шапка + две секции |
| 36 | [36-dlq-reprocessor.md](36-dlq-reprocessor.md) | Авто-репроцессор DLQ (развивает §34.4/§35) — фоновый периодический sweeper в Sender повторно доставляет неудачные async-сообщения из `nexus.async.dlq` до per-node TTL (`dlq_ttl_seconds`, дефолт 24ч); уважает circuit breaker, tombstones, статус узла; republish-в-хвост, backoff = интервал прохода; финальный лог `ttl_expired`; глобальные флаги `reprocess_enabled/interval/max_scan`; UI-поле TTL + подсказка; метрики |
| 37 | [37-node-id-in-logs.md](37-node-id-in-logs.md) | Идентификатор узла в логах — колонка `node_id` (UUID) в CH-таблице: per-node атрибуция для узлов, делящих одну таблицу (счётчики/графики/очистка/replay фильтруют по узлу, `DeleteFailed` не задевает чужие записи). Sender пишет node_id (sync через gRPC, async/DLQ локально); миграция существующих таблиц (`ALTER ADD COLUMN` в Web+Sender); read-фильтр `(node_id=? OR node_id='')` (legacy-совместимость); node id в UI «Конфиг» |
| 38 | [38-clog-retry-kafka.md](38-clog-retry-kafka.md) | Durable-retry проваленных CH-батчей через Kafka (замена NDJSON-fallback) — при недоступности ClickHouse Sender продьюсит проваленный батч в топик `nexus.logs.retry`, отдельная группа `<group>-clog-retry` дренит его обратно в CH (retry-in-place с бэкоффом до восстановления). Реплицируемо, без локальных файлов; Kafka нагружается только во время простоя CH (сообщение на батч, не на запрос). Удалён `clickhouse.fallback_dir`, добавлен `kafka.retry_topic`. Отправка sync/async не блокируется, ошибки CH по-прежнему в Sentry |
| 39 | [39-path-passthrough.md](39-path-passthrough.md) | Path-passthrough — опциональный флаг узла `path_passthrough`: хвост входящего пути после пути узла приклеивается к `target_url` (longest-prefix-резолв + `JoinPath`, traversal-safe; оба URL-режима; sync/async/callback). По умолчанию off (точный матч сохранён). Лог-колонки разведены: `http_method` (глагол, новая, после `type`) и `method` (репурпозен → подпуть запроса, напр. `v1/GetParcelsInfo`); проброс `request_path` через gRPC/envelope, идемпотентная миграция CH-таблиц в Web+Sender. Replay passthrough-записи по полному подпути. UI: toggle в форме + колонки в логах |
| 40 | [40-any-http-method.md](40-any-http-method.md) | HTTP-метод «Любой» (`ANY`) для входящего и исходящего метода узла. Вх=ANY — узел принимает запрос любым методом (без 405). Исх=ANY — Sender зеркалит метод входящего запроса (PUT→PUT); pull-узлы → POST. Резолв `effectiveOutgoingMethod` в Receiver; replay ANY-узла по залогированному `http_method`. Миграция 0020 (CHECK +ANY), DTO `oneof …ANY`, UI-пункт «Любой». По умолчанию ничего не меняется |
| 41 | [41-universal-request-auth.md](41-universal-request-auth.md) | Универсальная динамическая авторизация — источник (заголовок/параметр) + имя поля для **входящей** (token/basic, колонки `incoming_auth_dynamic_*`, миграция 0021) и **исходящей** (`*_from_request`) авторизации; новый каталог «полей запроса» (`request_fields_catalog`, миграция 0022, `/api/request-fields`) с обязательным выбором поля. Умный дедуп схемы (`Bearer <jwt>` не удваивается; кейс `?Bearer=Bearer+<jwt>`); пустое поле → исход без `Authorization` (не 401), вход → 401. Пересмотр статуса **«Down»** в Overview: по исходу ПОСЛЕДНЕГО вызова (gauge `nexus_node_last_request_error`), а не доле ошибок за период |
| 43 | [43-body-size-hard-limit.md](43-body-size-hard-limit.md) | Жёсткий лимит размера тела — при `max_body_size_enabled` тело > лимита больше НЕ усекается тихо (§22.2 superseded): sync отклоняется (413 запрос / 502 ответ), async не доставляется; во всех случаях запись лога `done=0` + reason, тело не пишется, checksum по полному. Единая точка `SendUsecase.Send` (sync+async), CB по реальному статусу upstream. Тесты unit+integration |
| 42 | [42-log-body-streaming.md](42-log-body-streaming.md) | Динамическая подгрузка тел логов — фикс зависания вкладки «Логи» на большом теле. `Get` отдаёт **превью** (первые 64K рун, нарезка `substringUTF8` в CH) + полные длины `request_len`/`response_len`; остаток — срезами по рунам через `GET …/log/{logId}/body` (`which/offset/limit`, `eof`); очень большое — потоковым скачиванием `…/body/download` (`text/plain` attachment). Порт `LogReader.GetByIDPreview`/`GetBodyChunk` (`which` whitelist), `GetByID` сохранён для replay. UI: «показать весь» (с предупреждением >2 МБ) / «скачать» / «копировать»; `prettyMaybe` не парсит тела >256K (убирает синхронный фриз). Без миграций/ENV |
| 44 | [44-dashboard-counters.md](44-dashboard-counters.md) | Счётчики дашборда: **шапка = Σ строк таблицы** за выбранный период из ClickHouse (уникальные запросы), а не из Prometheus (попытки) — фикс расхождения шапки/таблицы; `error_rate=errors/incoming`. Дефолтный период 24ч + сохранение «под себя» (localStorage). Тоггл паузы автообновления + интервал в `app_settings` (`metrics_refetch_ms`, `/api/settings/public`). Процент ошибок по каждому узлу. Диагностический `GET /api/metrics/diagnostics` (Prometheus↔ClickHouse сверка). Дата+время в логах (`DD.MM HH:MM:SS`). Колонка «Команды» в `Settings → Users` (батч `ListTeamsByUsers`, warn-чип рассинхрона). Смежно: резолв current_team по членству (§18.9), невалидный UUID `:id`→404 (Sentry NEXUS-7). Keyset-пагинация логов `(date_request, ID)` — фикс стопора скролла на плотных секундах. Режим подсчёта уникальных точно/приблизительно (`metrics_approx_counts`, HLL ~3× быстрее). Скил `nexus-prod` |
| 45 | [45-user-default-team.md](45-user-default-team.md) | Смена **команды по умолчанию** пользователя прямо в списке `Settings → Users` — клик по чипу команды в колонке «Команды». `PUT /api/users/:id/default-team` (admin) с валидацией членства (нельзя сделать дефолтной чужую команду → 400). Связан с §18.9 (резолв current_team по членству) и §44.G (колонка «Команды», warn-чип рассинхрона) |
| 46 | [46-node-status-redis.md](46-node-status-redis.md) | **Персистентный статус «Down» узла через Redis** (переживает рестарт). §41-гаудж `nexus_node_last_request_error` in-memory — теряется при деплое, и все узлы ложно показывают «OK». Sender пишет исход последнего вызова в Redis (`nexus:node:last_error:<path>`), Web читает оттуда (fallback на Prometheus). Подтверждено на бою: statusnpd «OK» после деплоя, хотя последний запрос упал |
| 47 | [47-logs-filter-refinements.md](47-logs-filter-refinements.md) | **Доработка фильтра логов узла и деталей лога** (развивает §33.4/§42/§44). §47.1 — клик по столбцу графика по сегментам (баг-фикс §33.4): красный сегмент → логи `status=err`, синий → `status=all` за окно бакета; было `onlyErrors: errors>0` → бакет с любой ошибкой открывал только ошибки (4 из 19). §47.2 — поле «Параметры» в детали лога над телами request/response, лениво из уже отдаваемого `parameters`, скрыто при пустом. §47.3 — убран per-node процент ошибок из Overview (частичный реверс §44); общий `error_rate` в шапке сохранён. §47.4 — кликабельный спарклайн на карточке узла дашборда: клик по столбцу открывает логи узла за окно бакета (дип-линк `/nodes/:id?tab=logs&from&to`, `useSearchParams` в NodeDetail), как клик по графику узла |
| 48 | [48-log-search-extended.md](48-log-search-extended.md) | **Расширенный поиск по логам узла (log-search v2)** (развивает §7.4/§42/§47). Мини-язык `q`: `&`=И, `\|`=ИЛИ, `-`=НЕ, префиксы `url:`/`params:`/`req:`/`resp:`, `\`-экранирование; поиск теперь и по колонке `parameters`. Кнопки-тумблеры в поле как в VS Code: Aa (регистр), ab\| (целое слово), .* (regex — весь ввод одно RE2, мини-язык отключён); невалидный regex → 400. Один парсер/матчер `internal/domain/logsearch` для SQL (CH `position*`/`match`) и in-memory зеркала live-tail. Новые фасеты: `GET /nodes/:id/logs/methods` (distinct `method`, лениво при открытии дропдауна) и `/logs/date-range` (min/max дат → `min`/`max` полей дат, лениво при фокусе). Поля IP/Host убраны из UI (API-параметры сохранены). Анализ производительности: field-scoped термы не читают тела, keyset+LIMIT, skip-индексы — future work |
| 49 | [49-favorite-teams.md](49-favorite-teams.md) | **Избранные команды** (развивает §18/§45). Отображаемое имя команды без slug в шапке и списке переключателя (кастомный Popover вместо `<select>`); звезда «в избранное» возле команды; секция «Избранное» в сайдбаре — клик переключает команду, drag-and-drop (`@dnd-kit`) меняет порядок. Хранение пер-пользовательское в PG: `user_team_favorites(user_id, team_id, position)` с составным FK на `user_teams` ON DELETE CASCADE (инвариант «избранное ⊆ членство»); `favorites` в `GET /api/me/teams` + `PUT /api/me/favorite-teams` (full-replace, session-only) |
| 50 | [50-node-cache-teamslug-redirects.md](50-node-cache-teamslug-redirects.md) | **Кеш узла по реальной команде + видимость редиректов + breaker** (боевой баг + §18/§9.2). Web писал/инвалидировал ключ кеша `node:default:<path>`, а Receiver читает `node:<team_slug>:<path>` — для узла вне команды default правки (target_url, статус, **удаление**, перенос) не доходили до Receiver до Redis-TTL (до 5 мин). Фикс: ключ по реальному slug (`resolveCacheTeamSlug`), креды в кеше **шифруются** (Receiver делает Decrypt). Видимость 3xx-редиректов Sender: `CheckRedirect` логирует хопы (Info; Warn при POST→GET с потерей тела), дописывает сводку в `reason` лога (видно в UI «Причина»), `redactURL` режет query; следование редиректам оставлено. §50.4: circuit breaker не открывается по 4xx (инцидент site/push — 422 NotRegistered блокировали валидные пуши) |
| 51 | [51-service-logging.md](51-service-logging.md) | **Управление логированием сервисов** (ТЗ зафиксировано, реализация не начата). Служебные slog-логи всех трёх процессов в UI: консоль follow-tail отдельным пунктом сайдбара «Логи» (admin) — чипсы сервисов, поиск, Live с паузой, скачивание; runtime-смена уровня без рестарта (`LevelVar` в собственной сборке цепочки хендлеров + `app_settings.logging.level` + reload-шина на все сервисы); доставка через Redis-кольцо `nexus:logs:<service>` (LPUSH+LTRIM+EXPIRE, неблокирующий шиппер, маскировка sensitiveKeys). SysLog/SSE/персистентное хранилище — вне scope |
| 52 | [52-node-degraded-status.md](52-node-degraded-status.md) | **Трёхсостоянье статуса узла OK / Degraded / Down** (развивает §41/§46/§50.4). Продолжение инцидента site/push: серия 422 красила живой узел в Down (флаг «последний вызов не-2xx» булев). Новое: `domain.NodeOutcome` — ok (2xx) / degraded (не-2xx < 500, жёлтый бейдж) / down (транспорт или 5xx); граница = upstreamHealthy §50.4. Gauge `nexus_node_last_request_error` расширен до 0/1/2 (max по репликам = худшее), Redis-значение — намеренно другая кодировка `"1"`=down/`"2"`=degraded (legacy/rolling-совместимость, матрица в разделе). API: `last_outcome` аддитивно, `last_error` back-compat. Счётчики ошибок §44 и `incomplete_total` не меняются |
| 53 | [53-copy-node.md](53-copy-node.md) | **Копирование узла** (развивает §3.3/§5.5/§18/§23/§26). `POST /api/nodes/{id}/copy` (manager+): серверный клон всех настроек **включая креды** (decrypt→re-encrypt внутри бэкенда, через API не проходят) с новым `path` в той же команде; копия всегда `paused`; ссылки allowlist-хостов клонируются в той же UoW-транзакции со снимком; аудит `node.copy` с провенансом source_node_id; CH-таблица сохраняется (§37 атрибутирует по node_id). UI: кнопка «Копировать» в шапке NodeDetail + диалог с префиллом `<path>-copy` |
| 55 | [55-dry-run-real-call.md](55-dry-run-real-call.md) | **Dry-run: реальный вызов target** (развивает §7.5.1/§4/§23/§32.2/§9.5/§46/§52). Боевой инцидент: узел висел 30с, а dry-run упирался в `auth.incoming` и не доходил до сути. Диалог доведён до §7.5.1: chips «Заголовки»/«Query» + подсказка, какое поле ждёт узел, чекбокс mock (вкл по умолчанию) + предупреждение при снятии; кнопка «Тестовый запрос» ещё и на странице просмотра узла (manager+). Реальный режим идёт **через Sender по gRPC** (тот же путь и клиент, что боевой трафик: редиректы §50, retry, лимит тела §43) — не своим клиентом Web (другой контейнер → ложный успех) и не через Receiver (тот резолвит узел из БД, а конфиг несохранён). Новое поле `bool dry_run = 30` в `SendRequest` гасит побочку: метрики §6, gauge §41/§52, персист статуса §46 и **circuit breaker §9.5/§50.4** (иначе тест по мёртвому адресу красил бы живой узел в Down и отдавал 503 боевому трафику); CH — двойной гейт `logging_enabled && !dry_run`; страховка `node_path=__dryrun_<path узла>`. Безопасность: реальный режим проходит self-reference §32.2 (сейчас его в dry-run нет) и allowlist §23; аудит различает mock/реальный. Порядок деплоя: Sender → Web |
| 54 | [54-overview-filter-persistence.md](54-overview-filter-persistence.md) | **Сохранение фильтров рабочего стола** (развивает §7.3/§28.4/§44.B). Баг-репорт: поиск/метод/статус/период сбрасывались при уходе на узел и возврате («Назад» браузера или кнопка «Узлы»). Гибрид: URL — источник истины (`?q=&method=&status=&range=` \| `from`+`to`, имена симметричны `periodParams()`), `sessionStorage` — зеркало на случай перехода, теряющего query (кнопка «Узлы» = `NavLink to="/"`). Сериализуются только отличия от дефолта → чистый URL при дефолтных фильтрах и живая звёздочка §44.B; выбран `sessionStorage`, а не `localStorage` (не вытесняет звёздочку, фильтр не «прилипает» на недели) и не PG (§49 — оверкилл, шаринг покрыт URL). Грабли: `save` строго до `setParams` (иначе очистка воскресает), debounce поиска ~300 мс (Safari троттлит `replaceState`), восстановление через `replace` (Back не ломается) |
| 56 | [56-ch-schema-sync.md](56-ch-schema-sync.md) | **Синхронизация схемы ClickHouse существующей таблицы (ALTER)** (развивает §19/§4.3/§22/§26; закрывает ловушку C.1). Смена шаблона/CH-настроек не применялась к уже созданной таблице (`CREATE TABLE IF NOT EXISTS` = no-op). §56 применяет её через `ALTER`: чистый планировщик `PlanSchemaSync` диффит целевой шаблон против снимка существующей таблицы (`system.columns`/`data_skipping_indices`/`tables`) и выдаёт ALTER-операторы (CODEC / ADD·DROP INDEX / MODIFY·REMOVE TTL) + список неприменимого (движок/ORDER BY/PARTITION BY → нужна пересоздача). Модель **предпросмотр + явное применение** (не побочка сохранения: ALTER'ы тяжёлые/частично необратимы): `POST /nodes/{id}/ch-schema/plan` (read-only DDL) → оператор видит точный DDL → `apply` (аудит `node.ch_schema_sync`, manager+). Нормализация CODEC — best-effort; `ADD INDEX` без авто-`MATERIALIZE` (историю оператор материализует сам) |
| 57 | [57-node-cache-invalidation.md](57-node-cache-invalidation.md) | **Гарантированная инвалидация конфига узла в Receiver** (развивает §9.2/§50/§51). Окно рассинхрона: write-through в Redis из Web молча становится no-op при провале резолва team-slug → старый конфиг (в т.ч. авторизация) живёт до TTL (до 300 с prod); L1-кеш Receiver'а Web инвалидировать не мог (до 2 с). Выделенный pub/sub канал `nexus:nodes:invalidate` ([platform/nodeevents](../../internal/platform/nodeevents/nodeevents.go)): Web публикует событие (team_id+path[+old_path]) после commit в Create/Update/Delete/Move/SetStatus (best-effort), Receiver подписан → резолвит team_id→slug → выселяет узел из L1 (`L2Reader.Invalidate`) и Redis (`DEL`), следующий запрос перечитывает свежий конфиг из PG. Публикация по team_id, а не slug: провал резолва slug на Web (тот же сбой, что ломает write-through) не блокирует инвалидацию — slug резолвит Receiver. Остаточное окно — только полный отказ Redis в момент публикации (TTL-страховка) |
| 58 | [58-node-share.md](58-node-share.md) | **Поделиться узлом** (развивает §18/§26/§53/§7.4). Кнопка «Поделиться» копирует ссылку на страницу узла в UI (`${origin}/nodes/{id}`, team-независимую) — на просмотре и в форме правки, доступна всем ролям. При открытии такой ссылки из другой команды сессия авто-переключается на команду узла: резолвер `GET /api/nodes/{id}/team` (session-only, проверяет членство) сообщает команду узла ДО загрузки самого узла (team-scoped `GET` вернул бы 404), хук `useEnsureNodeTeam` один раз переключает команду (`POST /api/me/switch-team`) и залипает на `ready`. Если команда узла пользователю недоступна → «Узел не доступен» (no-leak 404). Аудит прав кнопок узла: закрыта дыра — Replay был доступен `viewer` (группа `authed` + кнопка без гейта), перенесён в `authedManager`+`RequireSessionOnly`, на фронте (LogsTab/QueueTab) disabled для viewer |
| 59 | [59-nodes-ui-charts-config.md](59-nodes-ui-charts-config.md) | **Мини-графики в списке узлов, перенос узла из формы, конфиг узла и копирование URL лога** (развивает §22/§18/§21/§47, frontend-only). Табличное представление списка узлов получает мини-график трафика вместо кнопки «Перенести» — тот же `Sparkline` и те же данные `/api/metrics/nodes`, что в карточках (новых запросов нет), с тултипом по бакету и кликом → логи за окно бакета. Кнопка «Перенести» убрана из обоих представлений и переехала в форму правки узла между «Отмена» и «Поделиться» (admin-гейт сохранён; после переноса — уход на список, т.к. узел стал чужим). Read-only вкладка «Конфиг узла» дополнена командой-владельцем (имя резолвится по членствам — API узла отдаёт только `team_id`) и списком передаваемых заголовков. В журнале запросов полный URL стал копируемым: при наведении на обрезанную ячейку всплывает блок с адресом и кнопкой «Скопировать» — на popover с ручным управлением, т.к. обычный tooltip в плотной таблице закрывался от наведения на соседнюю строку и клик уходил в раскрытие записи (проверено на стенде). Проверено и подтверждено без правок кода: чтение логов прекращается при закрытии консоли/уходе со страницы |

| 60 | [60-node-move-shared-table.md](60-node-move-shared-table.md) | **Перенос узла не утаскивает общую таблицу логов** (чинит дефект §18 Move). `RENAME TABLE` выполнялся безусловно, хотя `clickhouse_table` штатно делит семейство узлов одного сервиса: перенос одного узла уносил общую таблицу в БД целевой команды, а остальные оставались со ссылкой на несуществующую — логирование ломалось молча, наружу шло только `code: 60, Unknown table` (на стенде перенос 1 узла из 176 обезглавил 175). Теперь считается число других узлов на той же таблице (по всем командам, без team-scope): личная — переезжает как раньше; общая — остаётся у прежней команды вместе с историей, а переехавшему узлу создаётся своя таблица по его шаблону. Попутно: занятое имя в целевой БД больше не роняет `RENAME` (пропуск, узел пишет в существующую). PG-запись остаётся авторитетной, все CH-операции — best-effort; при неизвестном статусе таблицы поведение прежнее |

| 63 | [63-node-author.md](63-node-author.md) | **Автор создания и последнего изменения узла** (развивает §21/§26/§59). В инфо-вкладке «Конфиг» узла — строка «Создано» (дата + `Автор: <логин создателя>`, всегда) выше строки «Обновлено» (дата + `Автор: <логин редактора>`, показывается только если узел меняли: критерий `updated_at ≠ created_at`). Источник — две новые колонки-логина `nodes.created_by`/`updated_by` (миграция 0026, `VARCHAR`, образец `node_allowed_hosts.created_by`), пишутся в тех же UPDATE/INSERT, что бампают `created_at`/`updated_at` (`Create`/`Copy`→created_by; `Update`/`SetStatus`/`Move`/allowlist-snapshot→updated_by; сигнатура `UpdateAllowedHostsSnapshot` +`updatedBy`). Не аудит-лог (retention чистит, /api/audit только manager+, доп. запрос на Get) — автор всегда в лад с датой, виден всем ролям (`NodeResponse`, логин не секрет). Без бэкфилла: у узлов до 0026 «Автор: —» |

| 62 | [62-global-node-search.md](62-global-node-search.md) | **Глобальный поиск узлов + история поиска** (развивает §18/§26/§49/§54/§58). Поле поиска в шапке ищет узлы во ВСЕХ командах пользователя (`GET /api/search/nodes`, session-only, `ListNodesFilter.TeamIDs` → `team_id = ANY`, обход членств как §58, обогащение имён команд из `ListUserTeams` без лишних запросов); выбор результата → `navigate(/nodes/:id)`, команду переключает существующий `useEnsureNodeTeam` §58; введённый `q` не очищается и переживает «Назад» через `sessionStorage` (`nexus.globalsearch.q`, не URL — топбар глобален). Префикс `/api/search/*`, а не `/nodes/search` (конфликт static-сегмента с wildcard `:id` в gin). История поиска — последние 10 строк на пользователя в PG (`user_search_history`, миграция 0025, образец §49; FK на `users` ON DELETE CASCADE; дедуп PK+`ON CONFLICT`; cap+trim в usecase; **без audit** — высокочастотно), ОДНА общая для глобального поиска и поля «Поиск» Overview; API `GET/POST/DELETE /api/me/search-history` (session-only, `user_id` из сессии); триггеры записи — выбор результата / Enter / blur непустого (не префиксы) |

| 61 | [61-log-attribution-and-move-preview.md](61-log-attribution-and-move-preview.md) | **Атрибуция записей на общей таблице логов и предпросмотр переноса** (продолжение §60, развивает §37/§18/§7.4). Фильтр `node_id = <id> OR node_id = ''` засчитывал записи без идентификатора ЛЮБОМУ узлу таблицы: на общей таблице узел видел чужие строки, после переноса — из другой команды (на стенде у переехавшего узла «появились» 590 201 чужая строка). Теперь послабление действует только на личной таблице; на общей — строго свой `node_id`; статус неизвестен → прежнее мягкое правило. Цена: карта «таблица → число узлов» считается ОДНИМ запросом, кешируется на минуту, обновляется вне блокировки в одной горутине, после неудачи не повторяется чаще 15 с (иначе лежащая БД получала бы запрос на каждое обращение к логам) и переживает отмену запроса пользователя. Плюс `GET /api/nodes/{id}/move-preview`: сообщает ДО переноса, что таблица общая либо что имя в целевой команде занято (узел подключится к чужим данным, а не начнёт с пустой) — диалог переноса показывает это предупреждением, сам перенос дублирует в лог |

| 64 | [64-manual-external-table.md](64-manual-external-table.md) | **Ручная (внешняя) таблица логов ClickHouse** (развивает §19/§37/§56/§61). Режим «Nexus как фронт логирования»: в CH-таблицу пишет посторонний сервис, Nexus только читает логи и считает по ним счётчики. До раздела «ручная» таблица (пустой `clickhouse_template_id`) обслуживалась как обычная и была уязвима на четырёх путях — провижининг по дефолтному шаблону, стартовые ALTER'ы обоих сервисов с backfill-мутациями, §56-синхронизация и, самое опасное, housekeeping: `DROP PARTITION` сносит партицию ЦЕЛИКОМ, а не строки узла (дефолт retention 90 дней). Новый флаг `nodes.external_table` (миграция 0027) делает таблицу неприкосновенной на всех четырёх; перенос узла в другую команду перестаёт ребейзить её имя на БД команды (иначе узел ушёл бы на несуществующее имя, а таблица осталась без читателя). Та же миграция разово проставляет дефолтный шаблон legacy-узлам с пустым `template_id` — иначе смена семантики молча выключила бы им retention. Структуру внешней таблицы Nexus не правит, поэтому её пригодность проверяется явно: `POST /api/ch-tables/verify` сверяет колонки и типы с `RequiredLogColumns` (лишние колонки допускаются, `DateTime('UTC')` == `DateTime`, `Nullable`/`LowCardinality` — расхождение), работает и для несохранённого узла. Форма создания узла заодно получает разумные дефолты: шаблон по умолчанию вместо «ручной», редактируемый префикс `<БД команды>.` в имени таблицы и включённый лимит тела из `web.node_default_max_body_size` (50000) |

| 65 | [65-node-form-team-display.md](65-node-form-team-display.md) | **Команда узла на форме и представление «только имя»** (развивает §18/§21/§58/§59). Форма узла получает read-only строку `Команда: <имя>` вверху правой колонки (над «Предпросмотром»): правка — `team_name` из резолвера §58 (ключ `["node-team", id]` уже закеширован `useEnsureNodeTeam`, второго запроса нет), создание — имя текущей команды сессии из членств. Представление команды в UI узла унифицировано: **только имя** — на вкладке «Конфиг» убраны `(slug)` и фолбэк на сырой UUID (имя недоступно → «—»). Новых эндпоинтов нет |

| 66 | [66-user-display-name.md](66-user-display-name.md) | **Отображаемое имя пользователя** (развивает §7.1/§7.9/§26/§63). Обязательное поле «Имя»: колонка `users.name` (миграция 0028, разовый backfill `name = login` — администратор правит имена руками), `name` обязателен в `POST/PUT /api/users`, отдаётся в `userResponse`/`meResponse`/`teamMemberResponse`, поиск ILIKE и по имени. Интерфейс показывает имя вместо логина (сайдбар-чип, Users, участники команд; логин остаётся вторичной строкой на админ-экранах — это кредентиал). Подписи автора — снапшотом: `Session.Name` → `Actor.UserLogin = DisplayName()` (фолбэк логин для сессий до деплоя) → `nodes.created_by/updated_by` §63 и `user_audit.user_login` пишутся именем; резолв на чтении невозможен (users admin-only, пользователь удаляем). Старые записи остаются с логином (= имени на момент миграции). След переименования — в аудите (`name`+`prev_name`) |

| 67 | [67-client-host-rdns.md](67-client-host-rdns.md) | **Хост клиента в логах (reverse-DNS) + фильтр + счётчик «Всего»** (развивает §4.3/§42.10/§48/§64). Колонка `client_host String` **AFTER IP** в `RequiredLogColumns` (новые таблицы — из шаблона, существующие — стартовый ensure `EnsureClientHostColumn`; внешние §64 — ручной ALTER владельца, SQL обязателен в описании релиза). Заполняет Sender: порт `HostResolver` + `internal/platform/rdns` — `Lookup` с нулём I/O (только L1), промах → фоновый резолв (Redis-кеш `nexus:rdns:<ip>` с TTL позитив/негатив → PTR-lookup, singleflight, fail-open; `rabbitmq://…` §27.10 → пусто); конфиг `sender.rdns`. UI: фасет `GET /nodes/{id}/logs/client-hosts` + cmdk-фильтр «Хост клиента» во втором ряду расширенных фильтров (колонки в таблице логов НЕТ, в DTO записи не отдаётся); фильтр работает в snapshot/live/count. Счётчик «Показано N из M»: `GET /logs/count` — `count()` под теми же фильтрами (общий `searchConds` с Search, `before_id` игнорируется), таймаут 10с → мягкая деградация; в Live скрыт. Деплой-окно внешних таблиц: INSERT буферизуется в Kafka-retry §38, facet деградирует пустым списком (`isMissingColumnErr`), verify показывает Missing |

| 68 | [68-multipart-form-data.md](68-multipart-form-data.md) | **Поддержка multipart/form-data** (развивает §4.3/§22.2/§42.10/§7.4.1/§43). Полная проброска тела с вложениями отправитель→получатель во всех видах отправки (sync/async/RabbitMQAsync/dry-run) — тело везде непрозрачные байты, `Content-Type` с `boundary` дословно, кода не потребовалось. Размер сообщения уже честный: `request_size`/`response_size` считаются по полному телу до усечения (вложения учтены). В ClickHouse тело вложения НЕ хранится — вместо него §68-плейсхолдер со сводкой частей (имя поля/`filename`/тип/размер, без содержимого; потолки 50 частей / скан 1000; безопасный fallback при неразобранном теле), точка подстановки — `SendUsecase.Send` (`logBodyCopy`, симметрично для запроса и multipart-ответа). Multipart освобождён от пер-узлового `max_body_size` (тело в CH и так не пишется); действуют только транспортные капы (`receiver.max_body_bytes` 5 МиБ, Kafka ~10 МиБ). Replay multipart-записи отклоняется 422 (`ErrReplayBodyMultipart`, детект `IsMultipartLogPlaceholder`), ручной override тела разрешён; UI показывает warn-подсказку и дизейблит кнопку. Без миграций/новых конфиг-ключей. Ограничение: записи до §68 (сырое тело) детект не ловит |

| 69 | [69-queue-tab-and-target-url.md](69-queue-tab-and-target-url.md) | **Вкладка «Очередь» для всех типов узлов, схема `target_url`, пересборка адреса доставки** (развивает §34/§35/§36/§3.3–3.4). Разбор боевого инцидента 2026-07-27: узел сохранили с `target_url` без схемы → все доставки падали `unsupported protocol scheme ""`, а после правки конфига застрявшие async-сообщения повторялись ещё сутки, и очистить их из UI было нельзя. §69.1 — вкладка доступна у `request`/`requestAsync`/`RabbitMQAsync`; у sync секция «Ожидают отправки» и peek Kafka отсутствуют (очереди нет), «Неудачные доставки» с replay/очисткой работают у всех; тексты паузы/подсказок/подтверждений для sync отличаются по смыслу (запросы отклоняются, а не копятся; авто-повтора нет). §69.2 — при `url_mode=static` непустой `target_url` обязан быть абсолютным http(s)-адресом с хостом (`ErrNodeTargetURLScheme` → 400 + `node.validation.target_url_scheme`, зеркало в `nodeValidation.ts`, `TrimSpace` в `SetDefaults`); при `from_request` не проверяется — поле скрыто, остаточное значение не должно блокировать сохранение; общий хелпер `domain.AbsoluteHTTPURL` переиспользован резолвером Receiver'а и `ValidatePublicBaseURL`. §69.3 — для static адрес доставки собирается заново перед каждой отправкой (свежий `target_url` + хвост path-passthrough + query конверта) в `buildSendInput`, поэтому чинятся все три пути (consumer, delay-топик §3.6, DLQ §36); `from_request` и сбой разбора → адрес из конверта; ограничение — правка query внутри `target_url` на застрявшие сообщения не действует. §69.4 — `List`/`purge` не сканируют топики для sync-узлов, `purge-failed` не пишет им tombstone'ы. Без миграций и новых конфиг-ключей |

| 70 | [70-multi-instance-clickhouse.md](70-multi-instance-clickhouse.md) | **Несколько нод Nexus на одном ClickHouse** (развивает §18/§19/§4.3/§56/§64/§14/§6). Нода = самостоятельное развёртывание со своими PostgreSQL/Redis/Kafka и общим хранилищем логов. Идентификатор `instance.id` (пусто = текущая нода, иначе 1–8 символов, тестовая — `kz`) разводит имена баз: `nexus_<id>_<slug>`; он же тег Sentry, метка `nexus_instance` и атрибут логов. Разведения имён мало (слаг допускает `_`, поэтому `kz_edo` и `kz`+`edo` дают одно имя), поэтому вводится **маркер владения** — служебная таблица `__nexus_owner` (`TinyLog`, невидима для поиска бесхозных таблиц) с атомарным захватом через `CREATE TABLE` без `IF NOT EXISTS`. Гейт закрывает всё разрушающее: `DROP PARTITION` housekeeping'а (сносил партицию целиком по чужому retention), удаление «бесхозных» таблиц, `ALTER` §56, стартовые `ADD COLUMN`/мутации размеров тел, `CREATE`/`RENAME` таблиц и баз, `DELETE` неудачных доставок, послабление `node_id=''` §61. Первый запуск с пустым `instance.id` при существующей `nexus_default` останавливается; смена идентификатора на живой ноде запрещена. Межнодовая работа разрешена только через внешнюю таблицу §64 — и там осознанно **и чтение, и запись**. Redis/Kafka/Prometheus разделять между нодами нельзя (pub/sub-каналы, имена топиков и групп, агрегация запросов панели) |

| 71 | [71-user-preferences.md](71-user-preferences.md) | **Персональные предпочтения пользователя: дефолтный период рабочего стола per-team** (развивает §44.B/§54/§18). Дефолтный период жил в `localStorage` (`nexus.overview.period`) — один ключ на браузерный профиль, без привязки к пользователю и команде: у оператора разные команды означают разные горизонты наблюдения, настройка не переезжала на другое устройство, а два человека за одной машиной перетирали дефолты друг другу. Вводится generic-хранилище `user_preferences` (`user_id`, nullable `team_id`, `key`, `value JSONB`, миграция 0031) + `GET/PUT /api/me/prefs`; первый ключ — `overview.period`, будущие префы (§48 «сохранённые фильтры per-user») ложатся туда новым `key` без миграции и правки бэкенда. уникальность даёт индекс по выражению `COALESCE(team_id, …)` — иначе upsert глобальных префов превращается в append; `UNIQUE NULLS NOT DISTINCT` не подходит (PostgreSQL 15+, а бой на 12); два FK (составной на `user_teams` для инварианта «преф ⊆ членство» и прямой на `users` для строк с `team_id IS NULL`). Значение непрозрачно для сервера, контракт держит клиент; чтение при сбое хранилища деградирует в пустой список, а не в 500 — рабочий стол ждёт префы перед запросом метрик. Резолв: преф команды → глобальный преф → системные 24ч. Переключение команды всегда даёт её период — сбрасывается даже выбранный вручную (первое появление `teamId` сбросом не считается, поэтому прямая ссылка `?range=…` при открытии уважается); в сессионное зеркало §54 период не пишется, так что возврат на «Узлы» тоже показывает дефолт команды. Прежнее localStorage-значение разово мигрирует в глобальный преф (удалить механизм через два релиза). Вне scope — вкладки узла и Kafka-монитор (остаются на 24ч), DELETE-эндпоинт |

| 72 | [72-logs-server-side-filter.md](72-logs-server-side-filter.md) | **Серверная фильтрация логов узла** (развивает §7.4/§35/§44/§48/§64/§67). Боевой инцидент: при фильтре «Ошибки» шапка показывала «Показано 3 из 33», скролл не шёл, остальные 30 ошибок посмотреть было нельзя — быстрые фильтры применялись в браузере к уже загруженной странице, а счётчик «из M» считался сервером по всей таблице. §72.1 — семантика приведена к полному дополнению (`ok = done=1 AND 200<=status<400`, `err = NOT ok`): до этого запись с 3xx не проходила НИ ОДИН фильтр и пропадала из UI, а незавершённая с кодом 2xx числилась успехом; предикат один на SQL (`condLogOK`), live-tail (`logRecordOK`) и подсветку строк (`isLogOK`), семантика `done` не тронута (на ней KPI §35 и счётчики §44). §72.2 — параметры запроса собирает единственный модуль `lib/logsQuery.ts` для списка, счётчика и SSE-стрима; `status`/`done` обязаны входить в `queryKey` (иначе react-query отдаёт прежний кеш и фильтр снова лишь прячет строки), клиентский пост-фильтр удалён, дип-линки §33.4/§35/§47.1 фильтруются первым же запросом, live-буфер чистится при смене фильтра. §72.3 — keyset-пагинация вынесена в общий хук `useInfiniteLogs`, «Неудачные доставки» вкладки «Очередь» (был жёсткий `limit=50` вообще без пагинации) листаются скроллом. §72.4 — временное окно дублируется по колонке партиционирования `date_create` с запасом ±1 день: замер 200 000 строк дал 200 000 прочитанных строк и 3 части против 8 192 и 1 части; гейт `external_table` §64 обязателен (integration-подтест показывает потерю записи без него), границы считаются в UTC, сужение действует только при заданном окне. §72.5 — возврат к верху схлопывает накопленные страницы (авто-рефетч перезапрашивал все 20 сразу); `maxPages` не подходит — ломает накопительный список §44.K. Без миграций и новых конфиг-ключей |
| 73 | [73-instances-registry.md](73-instances-registry.md) | **Реестр инстансов в интерфейсе** (развивает §70; опирается на §30/§34.3, §9.6, §26, §7.13). §70 ввёл понятие инстанса — самостоятельного развёртывания шины со своими PostgreSQL/Redis/Kafka, — но оставил его невидимым: каждая нода знает только себя (`instance_identity` — синглтон). Вкладка **Настройки → Инстансы** (admin-only) показывает соседние развёртывания: название, адрес-гиперссылка, код инстанса, версия, статус; переход в соседа — в новой вкладке. §73.1 — терминология: «узел» = маршрут шины (`domain.Node`), «инстанс» = развёртывание (`domain.PeerInstance`); раздел назван «Инстансы», а не «Ноды», чтобы не сливаться с разделом «Узлы». §73.3 — **токен не нужен и на удалённой стороне менять нечего**: версия и активность снимаются с уже публичных `GET /api/version` и `GET /ready`, опрашиваемых параллельно; опрашивает сервер, а не браузер (CORS не настроен, CSP задаёт `connect-src 'self'`, плюс снимается mixed-content). §73.4 — таблица `peer_instances` (миграция 0032) отдельно от `instance_identity`, без `team_id`, уникальность по `lower(base_url)`, кеш последней пробы для мгновенной отрисовки; адрес нормализуется и обязан быть origin'ом без пути и userinfo. §73.5 — семь admin-only маршрутов под session-cookie; недоступность соседа — его статус, а не ошибка запроса (200). §73.8 — границы защиты: редиректы не выполняются, тело читается ограниченным ридером, наружу идут только распарсенные поля (ни сырого тела, ни `checks` из `/ready`); приватные диапазоны намеренно НЕ блокируются — риск принимается ролью admin. Два конфиг-ключа: `web.instance_probe_timeout_ms`, `web.instance_probe_rate_limit_per_min` |
| 74 | [74-safe-rollback.md](74-safe-rollback.md) | **Безопасный откат на предыдущую версию** (развивает §5.1/§14/§6/§70). Стратегия эксплуатации 24/7 — «при критическом дефекте возвращаемся на предыдущую версию, потом чиним», — не работала: откаченный Web/Receiver вообще не стартовал (`no migration found for version 32` → `exit 1`), потому что `golang-migrate` требует, чтобы версия из `schema_migrations` существовала в каталоге миграций образа; тем же гейтом блокировался и `--migrate-down` старым образом, то есть оператор, успевший пересобрать старый тег, оставался без инструмента отката. §74.2 — контракт схемы (только аддитивные миграции, `ADD COLUMN` nullable либо `NOT NULL DEFAULT`, никаких `SELECT *`, удаление колонки — через два релиза по expand/contract): именно он делает откат кода безопасным без отката схемы. §74.3 — старт при схеме новее бинаря **разрешён по умолчанию** (без ключей конфига и флагов: правка `config.yml` на боевом сервере в аварии — те же потерянные минуты), но объявляется громко — запись уровня `error` (уровень выбран ради Sentry: порог `sentry.level` = error, `warn` туда не доедет), метрики `nexus_pg_schema_version`/`nexus_pg_schema_ahead` и алерт `NexusSchemaAheadOfBinary` при удержании >15 мин; `dirty` по-прежнему валит старт. §74.4 — `--migrate-force <N>` снимает `dirty` и объявляет версию, **SQL не выполняя** (схему доводит оператор), номер сверяется с каталогом. §74.5 — образы приложения версионируются перед обновлением (`scripts/deploy/images.sh tag/rollback/list`), иначе `up --build` перетирает `:latest` и откат превращается в пересборку трёх образов. §74.6 — строка «Откат: N миграций / без отката схемы» в каждом релизе CHANGELOG, значение готовит `scripts/release/rollback_info.py`. §74.7 — инварианты процедуры: down выполняется образом НОВОЙ версии до пересборки, при остановленных сервисах. Вне рамок — горизонтальное масштабирование (отдельное ТЗ), автоматический откат, откат ClickHouse/Kafka/Redis (не требуется). Без миграций |
| 75 | [75-topic-size-jmx.md](75-topic-size-jmx.md) | **Размер топиков Kafka на экране мониторинга** (развивает §31, опирается на §6/§13). Колонка «Размер» в таблице «Топики» с самого появления раздела показывала прочерк: поле `size_bytes` не заполнялось никогда, и вопрос «сколько занимает очередь» решался через `ssh` на брокер. §75.1 — разбор источников: админ-протокол через `segmentio/kafka-go` размер НЕ отдаёт (`DescribeLogDirs` в библиотеке нет ни методом, ни сообщением в `protocol/`), `kafka_exporter` его не экспонирует ВОВСЕ (только партиции/offsets/lag — то, что Nexus считает сам), единственный источник — JMX брокера (`kafka.log:type=Log,name=Size`) → метрика `kafka_log_log_size{topic,partition}` в именовании официального `examples/kafka-2_0_0.yml`. §75.2 — агент подключается **javaagent внутрь процесса брокера** (свой образ `deploy/docker/kafka.Dockerfile` + минимальный `deploy/kafka-jmx.yml` + `KAFKA_OPTS` в трёх compose + job `kafka-jmx` на `kafka:7071`): standalone-экспортёр требовал бы JMX RMI (фиксация `java.rmi.server.hostname`, второй порт) и стороннего образа, официального у jmx_exporter не существует. Эксплуатация: применение = пересоздание контейнера брокера (простой 30–60 с, `requestAsync` отвечает ошибкой), ошибка в конфиге агента роняет БРОКЕР (JVM не стартует с битым javaagent), образ Kafka становится собираемым (нужен Maven Central либо свой `JMX_AGENT_URL`), в версионировании образов §74.5 брокер не участвует. Отдельная грабля, найденная стендом: `KAFKA_OPTS` наследуют CLI-утилиты (`kafka-topics.sh` и пр.) и пытаются занять уже занятый порт 7071 → healthcheck обязан выглядеть как `KAFKA_OPTS= /opt/kafka/bin/kafka-topics.sh …`, иначе брокер вечно `unhealthy` и `sender` (`condition: service_healthy`) не стартует вообще. §75.3 — `PromMetrics.KafkaTopicSizes` (instant `sum by(topic)(kafka_log_log_size)`) домешивается в `TopicInfo` ДО записи в Redis-кеш (один запрос на TTL 30 с); величина суммирует ВСЕ реплики — «занято на дисках кластера», а не логический объём. Деградация поблочная: нет Prometheus/агента или чужая Kafka → нули и прежний прочерк. §75.4 — прочерк получает `title`-подсказку, иначе читается как «топик пуст». Вне рамок: `retention_ms` (источник — `DescribeConfigs`), история размера графиком, участие размера в порогах/алертах, установка `kafka_exporter`. Без миграций и новых ключей конфига |
| 76 | [76-team-share.md](76-team-share.md) | **Ссылка на команду: параметр `?team=<slug>` и кнопка «Поделиться»** (развивает §18/§58/§49/§54/§71, frontend-only). Активная команда жила только в серверной сессии (§18.3): переслать коллеге «рабочий стол команды X» было нечем, а два таба на `/` могли смотреть на разные команды неразличимо по URL. §76.2 — кнопка `Share2` в строке команды попапа team-switcher'а (не во вкладке `Настройки → Команды`: та admin-only, а шарить вправе любой участник) копирует `${origin}/?team=<slug>`; ссылка по **slug**, а не по id как в §58 — slug глобально уникален и неизменяем (`PUT /api/teams` принимает только `name`), поэтому читается человеком и не протухает. §76.3 — параметр живёт на `/`, `/kafka`, `/audit` по белому списку; `/nodes/*` исключены (командой страницы владеет §58, два механизма подрались бы), `/settings/*` — вне скоупа команды (§7.14.1). §76.4 — семантика **зеркала**: значение параметра всегда равно slug'у текущей команды сессии, расхождение живёт только внутри окна применения ссылки; входящее значение применяется один раз (`POST /api/me/switch-team`), запись всегда `replace` и функциональной формой (чужие ключи фильтров §54 обязаны выжить). §76.5 — резолв slug→id клиентский по членствам из `GET /api/me/teams`: нового эндпоинта не нужно, а «нет такой команды» и «я не член» неотличимы by design (no-leak, как единый 404 §58.3); неудача (нет команды, 403 на switch) → неблокирующий баннер + приведение параметра к реальной команде. Грабли §76.7: приём `isFetchedAfterMount` из §58 неприменим (у `["me-teams"]` нет `refetchOnMount:"always"` → флаг навсегда `false`); зеркало заморожено на время своего же `switch-team`, иначе затирает саму применяемую ссылку; состояние баннера входит в зависимости эффекта, т.к. на ошибке членства остаются той же ссылкой (structural sharing). Без миграций, новых эндпоинтов и ключей конфига |

## Как пользоваться

- Открывайте конкретный раздел, чтобы не загружать весь файл.
- Внутри файлов сохранены оригинальные номера подпунктов (`### 3.1`, `### 7.4.1` и т.д.).
- Для общего обзора есть таблица выше.
- Исходный сводный документ `nexus_spec.md` остаётся источником правды и обновляется при правках разделов (при изменении раздела не забывайте синхронизировать сводный файл, либо генерируйте его сборщиком).

## Что уже реализовано

Карта реализации по разделам ТЗ — в [../IMPLEMENTATION.md](../IMPLEMENTATION.md).
Там же — ссылки на ключевые файлы кода, архитектурные решения и список «куда копать дальше».

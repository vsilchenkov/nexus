<!--
Шаблон описания GitLab-релиза (§74.5, DEPLOYMENT §9.5-E).

Как пользоваться:
  1. Скопировать этот файл: cp scripts/release/release_notes_template.md desc_1.0.0.md
  2. Вставить сверху секцию CHANGELOG выпускаемой версии (Added/Fixed/Changed + строка «Откат»).
  3. Подставить версии в блоки ниже (<новая>/<прежняя>), убрать неприменимые пункты.
  4. python scripts/release/create_release.py v1.0.0 "v1.0.0" desc_1.0.0.md

Блок «Обновление» обязателен в каждом релизе: сервер собирает образы сам, и шаг
сохранения текущих образов (images.sh tag) — единственное, что отличает откат за
секунды от пересборки трёх образов под нагрузкой.
-->

## Обновление

Выполняется на прод-сервере, из каталога проекта. Вариант C (корневой `docker-compose.yml`);
для вариантов A/B — те же команды со своим `-f` (см. DEPLOYMENT §9.1).

```bash
cd nexus

# 1. Сохранить текущие образы под версионным тегом — ОБЯЗАТЕЛЬНО ДО сборки.
#    Без этого `up --build` перетрёт nexus-*:latest, и откат превратится
#    в пересборку трёх образов (DEPLOYMENT §74.5).
./scripts/deploy/images.sh tag

# 2. (только если релиз добавляет миграции) дамп PostgreSQL — страховка отката.
docker compose exec -T postgres pg_dump -U nexus nexus > nexus_$(date +%F_%H%M).sql

# 3. Забрать тег и пересобрать сервисы.
git fetch --tags
git checkout v<новая>
git status --porcelain          # ДОЛЖНО быть пусто, иначе версия уедет как "-dirty"
docker compose up -d --build web receiver sender

# 4. Проверить.
curl -s http://<host>:8000/api/version     # → {"version":"<новая>"}
curl -s http://<host>:8000/health
```

Миграции применяются автоматически на старте `web`/`receiver`; отдельный шаг не нужен.

## Откат

```bash
./scripts/deploy/images.sh list                 # какие версии сохранены на хосте
./scripts/deploy/images.sh rollback <прежняя> --apply
git fetch --tags && git checkout v<прежняя>     # привести дерево к откаченной версии
```

Схему БД откатывать **не нужно**: она обратно совместима, откаченный код стартует на ней и
сообщает об этом логом уровня `error` + метрикой `nexus_pg_schema_ahead=1`. Исключение —
релиз, который удалял или переименовывал колонку (тогда это сказано в разделе «Откат» выше):
там сперва `docker compose run --rm web --migrate-down N` **образом новой версии и до
пересборки**, затем откат кода. Подробности — DEPLOYMENT §10.

## Особое в этом релизе

<!--
Оставить только то, что применимо; если ничего — убрать раздел целиком.

- **Порядок обновления сервисов:** … (например: сперва Sender, затем Receiver/Web —
  когда новое поле gRPC заполняет Sender, а читает Receiver).
- **Новые ключи config.yml:** … (файл переносится на бой вручную; без ключа работает дефолт —
  указать, какой).
- **Внешние таблицы §64:** SQL для владельцев, если релиз менял структуру лог-таблиц.
- **Ручные действия до/после выката:** … (например, привести значения узлов к новому CHECK).
-->

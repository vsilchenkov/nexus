-- 0029_instance_identity.down.sql
--
-- Откат §70.
DROP TABLE IF EXISTS instance_identity;

-- Прежнее ограничение (хвост 31 символ) НАМЕРЕННО не восстанавливается: если
-- на ноде уже созданы команды с суффиксом (nexus_kz_<slug>) или с длинным
-- слагом, старый CHECK не наложится и откат упадёт на середине. Ограничение
-- {0,40} безвредно для кода без §70 — оно только шире прежнего, а имена БД всё
-- равно строятся кодом, а не вводятся оператором.

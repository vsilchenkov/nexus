-- Откат §91.2 НЕВОЗМОЖЕН по данным: удалённые записи 'team.switch'
-- восстановить неоткуда (журнал append-only, копии не делается).
--
-- Схему миграция не меняла, поэтому откатывать здесь нечего. Код старой
-- версии снова начнёт писать 'team.switch' — новые записи появятся,
-- исторические останутся потерянными.
SELECT 1;

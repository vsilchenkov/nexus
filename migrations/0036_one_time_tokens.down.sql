-- 0036_one_time_tokens.down.sql
--
-- Откат §88.4.1.
--
-- ПОТЕРЯ ДАННЫХ: удаляются все выданные одноразовые ссылки. Практического
-- вреда нет — они короткоживущие (по умолчанию 60 минут) и восстановлению не
-- подлежат по замыслу: пользователь просто запрашивает письмо заново.
-- rollback_info.py пометит потерю сам (DROP TABLE).

DROP INDEX IF EXISTS users_email_lower_idx;
DROP TABLE IF EXISTS one_time_tokens;

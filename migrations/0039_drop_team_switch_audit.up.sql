-- §91.2: переключение команды больше не подотчётное действие — код перестал
-- писать 'team.switch', а накопленные записи вычищаются здесь.
--
-- Зачем удалять, а не ждать retention: переключение команды выполняется и
-- автоматически (переходы по ссылкам §76/§89.3), поэтому таких записей в
-- журнале больше, чем содержательных, и они вытесняют полезные события из
-- видимого окна выдачи.
--
-- ВНИМАНИЕ: необратимо. Down-миграция записи не восстанавливает — это учтено
-- строкой «Откат» в CHANGELOG (ТЗ §74.6).
DELETE FROM user_audit WHERE action = 'team.switch';

-- 0037_team_external_url.down.sql
--
-- Откат теряет заданные внешние ссылки команд (§89.4). Данные справочные —
-- маршрутизация от них не зависит, — но восстановить их можно только руками,
-- поэтому строка про потерю обязана попасть в CHANGELOG (§74.6).
ALTER TABLE teams DROP COLUMN IF EXISTS external_url;
